@@
-  <!-- this is the ASCII drift text at top -->
-  <AsciiHeading class="…">Mc::ocCM**+…</AsciiHeading>
+  {/* 
+    <AsciiHeading class="…">Mc::ocCM**+…</AsciiHeading>
+  */}@@
-  <!-- this is the ASCII drift text at top -->
-  <AsciiHeading class="…">Mc::ocCM**+…</AsciiHeading>
+  {/* 
+    <AsciiHeading class="…">Mc::ocCM**+…</AsciiHeading>
+  */}@@
-import AsciiHeading from './AsciiHeading.jsx';
+// import AsciiHeading from './AsciiHeading.jsx';@@
-import FlowNodesBackground from '../components/FlowNodesBackground.astro';
+// import FlowNodesBackground from '../components/FlowNodesBackground.astro';

@@
-  {/* <FlowNodesBackground /> */}
+  {/*
+    <FlowNodesBackground />
+  */}/* wipe out any leftover particle layers */
#particleCanvas,
.background-particles,
.flow-nodes-background {
  display: none !important;
}---
import "../global.css";
import TerminalFooter from '../components/TerminalFooter.astro';
import ThemeToggle from "../components/ThemeToggle.jsx";
const { title } = Astro.props;
---
<!DOCTYPE html>
<html lang="en" class="bg-bg-dark text-text-light">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title ? `${title} | AI Solutions` : 'AI Solutions Consulting'}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fira+Mono:wght@400;700&display=swap" rel="stylesheet">
  </head>
  <body class="font-sans antialiased bg-bg-dark text-text-light relative overflow-hidden min-h-screen">
    <!-- Skip to content link -->
    <a href="#main" class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-2 focus:bg-sky-600 focus:text-white focus:rounded">
      Skip to content
    </a>
    
    <header class="flex justify-end p-4">
      <ThemeToggle />
    </header>
    <!-- Floating particles (COMMENTED OUT) -->
    <!--
    <script is:inline>
      const chars = ['*', '+', '.', 'M', 'c', ':', 'N'];
      for (let i = 0; i < 50; i++) {
        const el = document.createElement('div');
        el.textContent = chars[Math.floor(Math.random() * chars.length)];
        el.style.position = 'fixed';
        el.style.left = Math.random() * 100 + 'vw';
        el.style.top = Math.random() * 100 + 'vh';
        el.style.color = 'var(--ascii-color)';
        el.style.fontFamily = 'monospace';
        el.style.opacity = 0.4;
        el.style.fontSize = Math.random() * 1.2 + 0.8 + 'rem';
        el.style.zIndex = '0';
        el.style.pointerEvents = 'none';
        el.setAttribute('aria-hidden', 'true');
        el.style.animation = `floatUp ${6 + Math.random() * 8}s linear infinite`;
        document.body.appendChild(el);
      }
    </script>
    <style>
      @keyframes floatUp {
        0% { transform: translateY(0); }
        100% { transform: translateY(-100vh); }
      }
    </style>
    -->

    <!-- Pixel Avatar -->
    <img src="/pixel-bot.gif" alt="Bot" class="fixed bottom-4 left-4 w-12 animate-bounce z-20" />

    <!-- ASCII Banner -->
    <pre class="text-[--ascii-color] text-sm md:text-base leading-none font-mono text-center mt-4 mb-2 glow select-none">
Mc:∘cCM**+.Nm::++NM+:Xm:.N.Mc:+
    </pre>

    <!-- Sticky Navigation -->
    <header class="fixed top-0 inset-x-0 z-50 bg-bg-dark/90 backdrop-blur border-b border-white/10">
      <nav class="mx-auto flex max-w-7xl items-center justify-center gap-10 py-4 text-lg font-medium">
        <a class="hover:underline hover:decoration-accent hover:decoration-2 transition-colors text-white" href="/" aria-current={Astro.url.pathname === '/' ? 'page' : undefined}>Home</a>
        <a class="hover:underline hover:decoration-accent hover:decoration-2 transition-colors text-white" href="/about" aria-current={Astro.url.pathname === '/about' ? 'page' : undefined}>About</a>
        <a class="hover:underline hover:decoration-accent hover:decoration-2 transition-colors text-white" href="/services" aria-current={Astro.url.pathname === '/services' ? 'page' : undefined}>Services</a>
        <a class="hover:underline hover:decoration-accent hover:decoration-2 transition-colors text-white" href="/blog" aria-current={Astro.url.pathname === '/blog' ? 'page' : undefined}>Blog</a>
        <button 
          onclick="toggleDarkMode()" 
          class="p-2 rounded-lg hover:bg-accent/10 focus:outline-accent transition-colors"
          aria-label="Toggle dark mode"
        >
          <svg class="w-5 h-5 dark:hidden" fill="currentColor" viewBox="0 0 20 20">
            <path d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z"/>
          </svg>
          <svg class="w-5 h-5 hidden dark:block" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 2a1 1 0 011 1v1a1 1 0 11-2 0V3a1 1 0 011-1zm4 8a4 4 0 11-8 0 4 4 0 018 0zm-.464 4.95l.707.707a1 1 0 001.414-1.414l-.707-.707a1 1 0 00-1.414 1.414zm2.12-10.607a1 1 0 010 1.414l-.706.707a1 1 0 11-1.414-1.414l.707-.707a1 1 0 011.414 0zM17 11a1 1 0 100-2h-1a1 1 0 100 2h1zm-7 4a1 1 0 011 1v1a1 1 0 11-2 0v-1a1 1 0 011-1zM5.05 6.464A1 1 0 106.465 5.05l-.708-.707a1 1 0 00-1.414 1.414l.707.707zm1.414 8.486l-.707.707a1 1 0 01-1.414-1.414l.707-.707a1 1 0 011.414 1.414zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z" clip-rule="evenodd"/>
          </svg>
        </button>
      </nav>
    </header>

    <!-- Main Content -->
    <main id="main" class="relative z-10 max-w-3xl mx-auto px-6 py-8 border-2 border-white/10 rounded-xl shadow-[0_0_20px_rgba(20,227,255,0.15)] bg-bg-dark pt-24">
      <slot />
    </main>
    <TerminalFooter />
    
    <!-- Dark mode toggle script -->
    <script is:inline>
      // Check for saved theme preference or default to light mode
      if (localStorage.theme === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
        document.documentElement.classList.add('dark');
      } else {
        document.documentElement.classList.remove('dark');
      }
      
      // Function to toggle dark mode
      window.toggleDarkMode = function() {
        if (document.documentElement.classList.contains('dark')) {
          document.documentElement.classList.remove('dark');
          localStorage.theme = 'light';
        } else {
          document.documentElement.classList.add('dark');
          localStorage.theme = 'dark';
        }
      };
    </script>
  </body>
</html>
